[transmogrifier]
pipeline = 
    data
    logger

[data]
blueprint = transmogrify.filesystem
directory = data

[print]
blueprint = transmogrify.printer

[logger]
blueprint = collective.transmogrifier.sections.logger
level = 1
name = logger
key = _path
